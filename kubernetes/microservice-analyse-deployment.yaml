apiVersion: apps/v1
kind: Deployment
metadata:
  name: microservice-analyse
  labels:
    app: microservice-analyse
spec:
  replicas: 2
  selector:
    matchLabels:
      app: microservice-analyse
  template:
    metadata:
      labels:
        app: microservice-analyse
    spec:
      containers:
      - name: microservice-analyse
        image: irrigation/microservice-analyse:latest
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 8082
        env:
        - name: SPRING_CONFIG_IMPORT
          valueFrom:
            configMapKeyRef:
              name: irrigation-config
              key: CONFIG_SERVER_URL
        - name: EUREKA_CLIENT_SERVICEURL_DEFAULTZONE
          valueFrom:
            configMapKeyRef:
              name: irrigation-config
              key: EUREKA_SERVER_URL
        - name: SPRING_KAFKA_BOOTSTRAP_SERVERS
          valueFrom:
            configMapKeyRef:
              name: irrigation-config
              key: KAFKA_BOOTSTRAP_SERVERS
---
apiVersion: v1
kind: Service
metadata:
  name: microservice-analyse
spec:
  selector:
    app: microservice-analyse
  ports:
  - protocol: TCP
    port: 8082
    targetPort: 8082
  type: ClusterIP
